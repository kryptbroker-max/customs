# Copy this file to `.env` and fill with real credentials
# Do NOT commit your real .env to source control

# Server
PORT=3000

# Resend email settings
RESEND_API_KEY=re_3uy1R3xG_KX7DXFGrMbLWTJXMdPBXCZgt
FROM_EMAIL="Organization Name <noreply@yourdomain.com>"

# Admin inbox access token (required for /admin and /admin/api/*)
ADMIN_TOKEN=change-this-long-random-token

# Optional: require this secret header for inbound webhook requests
INBOUND_WEBHOOK_SECRET=optional-webhook-secret

# Optional: MongoDB connection string for persistent inbox storage
# If omitted, the app falls back to data/inbox.json
MONGODB_URI=mongodb+srv://pollunit:07058478793David@cluster0.whzbtfp.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Custom Domain Configuration
CUSTOM_DOMAIN=yourdomain.com
ORGANIZATION_NAME=Your Organization

# Render Deployment
PUBLIC_BASE_URL=https://customsmail.onrender.com
LOGO_PUBLIC_URL=https://customsmail.onrender.com/img/logo.png
